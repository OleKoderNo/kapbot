# KapBot

Discord bot for Kap / KapteinOle.

## Setup

1. `npm i`
2. Create `.env` with `DISCORD_TOKEN`, `CLIENT_ID`, `GUILD_ID`

## Deploy slash commands (dev guild)

`npm run deploy:commands`

## Run bot

`npm run dev`
